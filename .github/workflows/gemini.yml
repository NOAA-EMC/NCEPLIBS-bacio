name: ai-code-review
on: [push,pull_request]

jobs:
  ai-code-review:
    runs-on: ubuntu-latest

    steps:

    - name: "Code review"
      uses: AlexanderRichert-NOAA/ci-gemini-code-review@develop
      with:
        api-key: ${{ secrets.GEMINI_API_KEY }}
