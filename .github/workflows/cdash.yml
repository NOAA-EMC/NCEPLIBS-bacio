name: cdash
on:
  push:
    branches:
    - develop
    - add_cdash

jobs:
  cdash:
    runs-on: ubuntu-latest

    steps:

    - name: CDash
      env:
        FC: gfortran
        CC: gcc
        CDASH_TOKEN: ${{ secrets.CDASH_TOKEN }}
      uses: NOAA-EMC/ci-cdash@develop
